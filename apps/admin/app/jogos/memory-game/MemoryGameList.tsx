'use client';

import { Typography } from 'antd';

export const MemoryGameList = () => {
	return (
		<Typography.Title level={1} className='mb-10 mt-4'>
			Jogo da Memória
		</Typography.Title>
	);
};
